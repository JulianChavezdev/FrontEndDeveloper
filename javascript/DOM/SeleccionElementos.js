/*

Para obtener elementos del DOM en JavaScript, puedes usar las siguientes formas:

Por ID: document.getElementById('miId') - Devuelve el elemento único con el ID especificado.

Por clase: document.getElementsByClassName('miClase') - Devuelve una colección de todos los elementos que tienen la clase especificada.

Por etiqueta: document.getElementsByTagName('miEtiqueta') - Devuelve una colección de elementos que coinciden con la etiqueta indicada.

Selector CSS: document.querySelector('selectorCSS') - Devuelve el primer elemento que coincide con el selector CSS.

Selector CSS para múltiples elementos: document.querySelectorAll('selectorCSS') - Devuelve todos los elementos que coinciden con el selector CSS.

Estas son las maneras más comunes de seleccionar elementos, facilitando la manipulación del DOM.e");
ta");





 */

/* EJEMPLOS */

document.getElementById("app-title")

document.querySelector("#app-title");

document.querySelector("p");

document.getElementsByClassName("menu-items");

document.getElementsByTagName("p");

document.querySelectorAll(".menu-items");